# Project Rules

## Git Commits

- Commit messages must include `Co-Authored-By:` with the local git author (from `git config user.name` and `git config user.email`)
- Do NOT use `Co-Authored-By: Claude` or any AI attribution
