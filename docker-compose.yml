services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: javaclaw
      POSTGRES_USER: javaclaw
      POSTGRES_PASSWORD: javaclaw
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
